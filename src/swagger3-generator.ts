import { CommonGenerator } from "./common";
import Swagger3 from "../types/swagger3";
import { Schema } from "../types/schema";

export class Swagger3Generator extends CommonGenerator{
  generate (swagger: Swagger3.Swagger) {
    return []
  }
}